{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "release": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "rimraf": "^3.0.2"
  }
}
